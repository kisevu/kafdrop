spring:
  application:
    name: kafka-recap
  kafka:
    producer:
      bootstrap-servers: localhost:9092
    consumer:
      bootstrap-servers: localhost:9092
      group-id: ameda-group-2024




server:
  port: 8383